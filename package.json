{
  "name": "onebeyond-phone-catalogue",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix api && npm install --prefix backend && npm install --prefix frontend",
    "build:api": "npm run build --prefix api",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build:api && npm run build:backend && npm run build:frontend",
    "dev:api": "npm run dev --prefix api",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=11.0.0"
  }
}
